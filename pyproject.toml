[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "hotel-booking"
version = "0.1.0"
description = "Hotel booking system laboratory project"
requires-python = ">=3.11"

[project.scripts]
hotel-booking = "hotel_booking.main:main"